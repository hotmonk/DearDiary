const auxillary = (props) => props.children;

export default auxillary;
//this is a higher order compnent that can be used instead of a div to wrap elements bcs jsx only alows one imeediete element while returning

import React from "react";
const layout = (props) => <main>{props.children}</main>;

export default layout;

//this is another layer between tthe components and app jsut incase we ever had to further customize stuff
